.container
  .info-block
    = @barman.email

  = link_to t(:remove_barman), point_barman_path(@current_point, id: @barman), class: 'item-heavy', method: :delete, data: {'modal-confirm' => t(:are_you_sure) }