.container
  .products
    - @products.each do |product|
      = link_to(edit_point_product_path(@point, product), class: 'item') do
        %div= product.title
        %div= "#{product.ml}ml / #{product.price} ₽"