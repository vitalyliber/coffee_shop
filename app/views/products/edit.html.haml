.container
  .products
    = render 'products/form'
    = button_to t(:remove_product), point_product_path(@point, @product), class: 'btn btn-default remove-button', method: :delete, data: {confirm: "Are you sure?"}